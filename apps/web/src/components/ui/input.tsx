/**
 * Compatibility entrypoint for callers that still import the shared Input
 * component from the web app's former local UI path.
 */
export { Input } from '@goblin/ui';
export type { InputProps, InputVariantProps } from '@goblin/ui';
