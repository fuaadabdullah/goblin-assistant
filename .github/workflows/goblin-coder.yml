name: Goblin Coder (Rovo Dev)

# Triggered by the backend via repository_dispatch when the goblin
# routes a code task to Rovo Dev. Never run manually in production.
on:
  repository_dispatch:
    types: [goblin-coder]

jobs:
  code:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # For autofix dispatches, check out the failing PR branch.
          # Falls back to the repo default branch for manual dispatches.
          ref: ${{ github.event.client_payload.branch || github.event.repository.default_branch }}

      - name: Run Rovo Dev
        uses: atlassian-labs/rovo-dev-action@v1
        with:
          prompt: ${{ github.event.client_payload.prompt }}
          atlassian_email: ${{ secrets.ATLASSIAN_EMAIL }}
          atlassian_token: ${{ secrets.ATLASSIAN_API_TOKEN }}

      - name: Report result
        if: always()
        run: |
          echo "task_id=${{ github.event.client_payload.task_id }}"
          echo "job=${{ github.event.client_payload.job }}"
          echo "branch=${{ github.event.client_payload.branch }}"
          echo "run_id=${{ github.run_id }}"
