# Goblin Assistant - Privacy & Security Features
# Add these packages to your existing requirements.txt

# Core privacy & security dependencies
redis>=5.0.0  # Rate limiting backend
chromadb>=0.4.0  # Vector store with TTL support
# torch>=2.0.0  # PyTorch for ML models (NOT AVAILABLE for Python 3.13 yet)
transformers>=4.30.0  # Hugging Face transformers (installed)
# sentence-transformers>=2.0.0  # For vector embeddings (requires torch - OPTIONAL)
datadog>=0.49.0  # Telemetry with redaction

# NOTE: Vector store features are OPTIONAL and require PyTorch
# PyTorch does not yet support Python 3.13 (as of Jan 2026)
# Options:
#   1. Use Python 3.11 or 3.12 for vector store features
#   2. Wait for PyTorch 3.13 support
#   3. Run without vector store (all other privacy features work fine)

# Already required (verify versions)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
supabase>=2.0.0

# Development & testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # For testing async endpoints
